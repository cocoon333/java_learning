public class BinarySearchST{
    public static void main(String[] args){
        //test will be added here

    }

    public static new(){
        SET<String> set = new SET<String>();
        Set keys = Set.SET();
        Set values = Set.SET();
    }

    public static set(String[] args){
        if (!contains(args[0])){
            keys.add(args[0]);
            values.add(args[1]);
        }
    }

    public static remove(String[] args){
        if (contains(args[0])){
            keys.remove(args[0]);
            values.remove(args[1]);
        }
    }

    public static contains(String[] args){
        String key = '';
        int i = keys.length()/2.0;
        while (args[0] != key){
            keys
